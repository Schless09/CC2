import ManagerIntakeClient from './ManagerIntakeClient';

const page = () => {
  return (
    <div className='flex flex-col items-center justify-center min-h-screen p-8 max-w-lg mx-auto'>
      <ManagerIntakeClient />
    </div>
  );
};

export default page;
